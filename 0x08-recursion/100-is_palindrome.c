#include "main.h"
/**
* _sqrt_recursion - function that returns 1 if a string is a palindrome and 0 if not
* @s: integer
* Return: returns 1 if a string is a palindrome and 0 if not
*/
int is_palindrome(char *s)
{
	
}
