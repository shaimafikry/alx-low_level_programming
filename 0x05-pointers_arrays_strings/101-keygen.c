#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry
 * generate valid password
 * Return: 0 Always success
 */

int main (void)
{
	srand(time(0));
	int password = rand();
	return (0);
}
