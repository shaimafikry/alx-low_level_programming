#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry
 * multiple two numbers
 * Return: return 0 success
 */
int main (void)
{
	unsigned long int num1, num2;
	unsigned long int mul = 0;

	mul = num1 * num2;
	printf("%lu",mul);
	
	return (0);
}
